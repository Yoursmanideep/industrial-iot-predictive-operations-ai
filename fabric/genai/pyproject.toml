[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "apex-industrial-iot-genai-rag"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "azure-search-documents>=11.6.0",
  "openai>=1.99.5",
  "PyYAML>=6.0"
]
