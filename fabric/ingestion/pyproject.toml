[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fabric-ingestion-control-plane"
version = "0.1.0"
description = "Replay-safe ingestion control plane for the Industrial IoT Fabric solution."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]